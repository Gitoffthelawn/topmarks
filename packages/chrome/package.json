{
  "name": "@topmarks/chrome",
  "version": "1.9.0",
  "private": true,
  "license": "MIT",
  "description": "Chrome new-tab extension that floats your bookmarks toolbar at the top with a rotating Unsplash wallpaper.",
  "scripts": {
    "build": "tsx build.ts",
    "dev": "tsx build.ts && (tsx build.ts --watch & web-ext run --source-dir=dist --target=chromium)",
    "package": "web-ext build"
  },
  "dependencies": {
    "@topmarks/shared": "*"
  },
  "devDependencies": {
    "web-ext": "^10.1.0"
  }
}
